machine:
  kubelet:
    extraMounts:
      - destination: /var/lib/longhorn
        type: bind
        source: /var/lib/longhorn
        options:
          - bind
          - rshared
          - rw
  sysctls:
    vm.overcommit_memory: "1"
    kernel.panic: "10"
    kernel.panic_on_oops: "1"
